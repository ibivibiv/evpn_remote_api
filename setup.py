from setuptools import setup

setup(
    name='evpn_remote_api',
    version='0.1',
    packages=['evpn_remote_api'],
    url='',
    license='none',
    author='haroldhannon',
    author_email='hhannon@us.ibm.com',
    description='quick evpn client for setting up bridges, vxlan interfaces, frr, and tap interfaces'
)
